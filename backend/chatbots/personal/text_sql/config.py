import os
from dotenv import load_dotenv

load_dotenv()

data_db = {
    "user": "postgres",
    "password": "Swoyeshzoro@2023",
    "database": "user_management",
    "host": "localhost",
    "port": 5432
}